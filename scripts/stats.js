document.addEventListener('DOMContentLoaded', ()=>{ loadPlayer().then(p=>{ const list = el('stats-list'); if(!list) return; list.innerHTML=''; const stats = [{id:'discipline',name:'Disciplina'},{id:'mana',name:'Mana'},{id:'attack',name:'Ataque'}]; stats.forEach(s=>{ const st = create('div','stat-row'); st.innerHTML = '<div><strong>'+s.name+'</strong></div><div style="width:140px;"><div class="stat-bar"><div class="stat-fill" style="width:'+ (Math.floor(Math.random()*60)+20) +'%"></div></div></div>'; list.appendChild(st); }); }); });