const DATA_KEY = 'agilrpg_v4_player'; function loadCSV(path){ return fetch(path).then(r=>r.text()).then(txt=>{ const lines = txt.trim().split('\n'); const headers = lines[0].split(','); const rows = []; for(let i=1;i<lines.length;i++){ const cols = lines[i].split(','); const obj={}; headers.forEach((h,idx)=> obj[h.trim()]= (cols[idx]||'').trim()); rows.push(obj);} return rows; }); } function loadPlayer(){ const raw = localStorage.getItem(DATA_KEY); if(raw) return Promise.resolve(JSON.parse(raw)); return fetch('data/player_inventory.json').then(r=>r.json()).catch(()=>({acquired:[],equipped:[],profile:{avatar:{name:'Protagonista'}}})); } function savePlayer(p){ localStorage.setItem(DATA_KEY, JSON.stringify(p)); }